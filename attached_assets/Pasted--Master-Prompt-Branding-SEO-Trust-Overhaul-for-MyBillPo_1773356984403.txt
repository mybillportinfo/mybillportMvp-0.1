🔥 Master Prompt: Branding, SEO & Trust Overhaul for MyBillPort

You are a senior branding expert and frontend developer. Your task is to transform the MyBillPort website into a trustworthy, SEO‑optimized, and visually compelling brand presence that converts visitors into users. The app already exists (Next.js, Tailwind CSS), but the website needs a professional upgrade.

🎯 Objectives

1. Logo & Brand Identity

· Design a modern, memorable logo that reflects the brand motto: "Never miss a bill again." It should convey trust, simplicity, and financial peace of mind.
· Provide at least two logo concepts (SVG format) with variations: full color, monochrome, favicon.
· Create a brand style guide (colors, typography, usage guidelines) that aligns with the existing Tailwind theme (or suggest improvements).

2. SEO Optimization

· Research target keywords (e.g., "bill management Canada", "pay bills all in one", "Canadian bill tracker", "never miss a payment", etc.).
· Generate optimized meta tags (title, description) for each key page: Home, Features, Pricing, About, Contact.
· Produce a sitemap.xml and robots.txt.
· Suggest content improvements for the homepage to rank better (e.g., add an H1 with primary keyword, include keyword‑rich subheadings, FAQ schema markup).
· Provide a structured data snippet (JSON‑LD) for the organization and website.

3. Trust & Credibility Elements

· Add trust badges (e.g., "256‑bit encryption", "Bank‑grade security", "100% Canadian", "GDPR compliant") with explanations.
· Create a testimonials section with placeholder testimonials that sound authentic (use AI‑generated quotes from satisfied beta users).
· Add a security & privacy summary (e.g., "Your data is encrypted and never sold").
· Include social proof (e.g., "Join 1,000+ Canadians who simplified their bills").

4. Implementation Details

· All changes must be in React/Next.js and Tailwind CSS.
· The logo should be output as an SVG component (or image) ready to drop into the existing layout.
· Provide code snippets for the updated homepage (app/page.tsx), meta tags in app/layout.tsx, and any new components (e.g., TrustBadges, Testimonials).
· Ensure the site remains mobile‑friendly and loads quickly.

🚀 Deliverables

· Logo SVGs (at least 2 concepts) + style guide.
· Updated homepage component with new hero, trust badges, testimonials, and SEO‑optimized copy.
· Meta tags and JSON‑LD for all pages.
· Sitemap.xml and robots.txt files.
· A brief SEO checklist and reasoning behind keyword choices.

Generate all outputs as copy‑paste‑ready code or files. Assume the project is a Next.js 14+ app with Tailwind CSS and TypeScript. If you need to create new components, place them in the appropriate directories (e.g., components/).

Now, execute this transformation. Make MyBillPort look like a serious fintech brand that users can trust at first glance.